<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cyanoboru.secrethitler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:debuggable="false"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".add_player" />
        <activity android:name=".show_role" />
        <activity android:name=".legislar" />
        <activity android:name=".InvestigarJugador" />
        <activity android:name=".MatarJugador" />
        <activity android:name=".ver3cartas" />
        <activity android:name=".vitoria"></activity>
    </application>

</manifest>